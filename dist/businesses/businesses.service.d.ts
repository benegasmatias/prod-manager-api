import { Repository, DataSource } from 'typeorm';
import { Business } from './entities/business.entity';
import { BusinessTemplateDto } from './dto/business-template.dto';
import { BusinessMembership } from './entities/business-membership.entity';
import { User } from '../users/entities/user.entity';
import { BusinessTemplate } from './entities/business-template.entity';
import { CreateBusinessFromTemplateDto } from './dto/create-business-from-template.dto';
import { Order } from '../orders/entities/order.entity';
import { Customer } from '../customers/entities/customer.entity';
import { Machine } from '../machines/entities/machine.entity';
import { Material } from '../materials/entities/material.entity';
import { DashboardSummaryDto } from './dto/dashboard-summary.dto';
import { UpdateBusinessDto } from './dto/update-business.dto';
import { Employee } from '../employees/entities/employee.entity';
import { BusinessStrategyProvider } from './strategies/business-strategy.provider';
import { PlanUsageService } from './plan-usage.service';
import { BillingService } from './billing.service';
import { AuditService } from '../audit/audit.service';
export declare class BusinessesService {
    private readonly businessRepository;
    private readonly membershipRepository;
    private readonly userRepository;
    private readonly templateRepository;
    private readonly orderRepository;
    private readonly customerRepository;
    private readonly machineRepository;
    private readonly materialRepository;
    private readonly employeeRepository;
    private readonly planUsageService;
    private readonly auditService;
    private readonly billingService;
    private readonly dataSource;
    private readonly strategyProvider;
    constructor(businessRepository: Repository<Business>, membershipRepository: Repository<BusinessMembership>, userRepository: Repository<User>, templateRepository: Repository<BusinessTemplate>, orderRepository: Repository<Order>, customerRepository: Repository<Customer>, machineRepository: Repository<Machine>, materialRepository: Repository<Material>, employeeRepository: Repository<Employee>, planUsageService: PlanUsageService, auditService: AuditService, billingService: BillingService, dataSource: DataSource, strategyProvider: BusinessStrategyProvider);
    getTemplates(userId?: string): Promise<BusinessTemplateDto[]>;
    private checkPlanAccessibility;
    createFromTemplate(userId: string, createDto: CreateBusinessFromTemplateDto): Promise<any>;
    updateOnboardingStep(userId: string, businessId: string, step: string): Promise<any>;
    activateBusiness(userId: string, businessId: string): Promise<any>;
    updateStatusAdmin(businessId: string, newStatus: string, reasonCode?: string, reasonText?: string): Promise<any>;
    updateEnabledAdmin(businessId: string, isEnabled: boolean, reasonCode?: string, reasonText?: string): Promise<any>;
    getBusinessAuditLogs(businessId: string): Promise<any>;
    getBusinessUsage(businessId: string): Promise<any>;
    resolveBusinessConfig(userId: string, businessId: string): Promise<any>;
    private getPermissionsForRole;
    getDashboardSummary(userId: string, businessId: string): Promise<DashboardSummaryDto>;
    findOne(userId: string, id: string): Promise<Business>;
    update(userId: string, id: string, updateDto: UpdateBusinessDto): Promise<Business>;
    findUserBusinesses(userId: string, filters?: any): Promise<Business[]>;
    checkAccess(userId: string, businessId: string): Promise<boolean>;
    addMemberToBusiness(userId: string, businessId: string, role: string): Promise<BusinessMembership>;
}
